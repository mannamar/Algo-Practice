// 542. 01 Matrix
// https://leetcode.com/problems/01-matrix/

var updateMatrix = function(mat) {
    // Create result matrix
    // Iterate over columns
        // Iterate over rows
            // Find nearest zero (call findNearestZero)
            // Set returned value in result array
    // Return result array
};

function findNearestZero(mat, m, n) {
    // Initialize counter
    // While 0 not found
        // Search counter spaces away for a 0
    // Return counter
}

// Will likely need to expand upon "Search counter spaces away for a 0"
    // Do you need to check diagonals? Or just up, down, left, right? 
// As outlined time complexity would be O(m^2 * n^2)?